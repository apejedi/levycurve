(ns levycurve.core
  (:gen-class
   :name levycurve.core
   :extends javax.swing.JFrame
   ))

(defn -main
  "Setup UI components
   Initialize Data Structure"
  [& args]
  (println "Hello, World!"))
